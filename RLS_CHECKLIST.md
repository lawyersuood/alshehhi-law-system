# قائمة التحقق: سياسات أمان الصفوف (RLS) المطلوبة في لوحة تحكم Supabase

هذا الملف **لا يُطبّق أي شيء تلقائياً** — هو دليل يدوي لصاحب النظام لتفعيل سياسات RLS
(Row Level Security) في لوحة تحكم Supabase (Database → Tables → كل جدول → RLS)، لأن هذا
الوكيل البرمجي لا يملك صلاحية الوصول للوحة التحكم. **يجب تطبيق هذه الخطوات يدوياً قبل الإطلاق
الفعلي للنظام**، وإلا فإن أي شخص يملك مفتاح anon key العام (المضمّن أصلاً في كود الموقع
الأمامي، وهو ليس سراً) يمكنه نظرياً قراءة/تعديل/حذف أي صف في أي جدول من هذه الجداول مباشرة عبر
واجهة برمجة Supabase، بصرف النظر عن أي صلاحيات تُفرض داخل واجهة المستخدم.

## الوضع الحالي (كما هو مكتشف من الكود)

كل الجداول التالية تُقرأ وتُكتب مباشرة من متصفح المستخدم عبر مكتبة Supabase JS (وليس عبر خادم
Express)، باستخدام الشكل الموحّد `{id, data}` حيث `data` هو كائن JSON يحتوي كامل السجل. أي
جدول لا يوجد له سياسة RLS مفعّلة (أو مفعّلة RLS لكن بلا أي policy) يكون إما مغلقاً بالكامل
(لا أحد يصل، بما فيهم التطبيق نفسه) أو مفتوحاً بالكامل لأي زائر يعرف رابط مشروع Supabase —
حسب الإعداد الافتراضي. **يجب التأكد من كل جدول أدناه أن RLS مفعّل ومعه Policy واضحة.**

## الجداول المتزامنة مع Supabase وما تحتاجه كل واحدة

| الجدول (اسم الجدول في Supabase) | المحتوى | السياسة الموصى بها |
|---|---|---|
| `clients` | بيانات الموكلين (أسماء، جهات اتصال، بيانات حساسة) | مستخدمون مسجّلو الدخول فقط (`authenticated`) من نفس المكتب — `USING (auth.role() = 'authenticated')` كحد أدنى، ويُفضّل لاحقاً تقييدها بعمود `firm_id` إن أُضيف تعدد مكاتب |
| `cases` | ملفات القضايا، الخصوم، تفاصيل الدعاوى | نفس سياسة `clients` — بيانات حساسة قانونياً |
| `fee_agreements` | اتفاقيات الأتعاب والمبالغ المتفق عليها | `authenticated` فقط — بيانات مالية |
| `payments` | سندات القبض والدفعات | `authenticated` فقط — بيانات مالية حساسة |
| `invoices` | الفواتير الضريبية الرئيسية | `authenticated` فقط — بيانات مالية حساسة، ويُفضَّل لاحقاً قصر الحذف (`DELETE`) على دور المحاسب/المدير فقط عبر policy منفصلة على عملية DELETE |
| `audit_logs` | سجل التدقيق لكل العمليات الحساسة | `authenticated` للقراءة، وتقييد `INSERT` فقط (بدون `UPDATE`/`DELETE` لأي أحد) — سجل تدقيق يجب ألا يُعدَّل أو يُحذف بعد كتابته لضمان مصداقيته |
| `kyc_items` | استمارات اعرف عميلك (KYC)، بيانات PEP والعقوبات (إقرار ذاتي) | `authenticated` فقط — بيانات امتثال حساسة جداً (انظر أيضاً تنبيه "إقرار ذاتي غير محقق" المضاف في الواجهة) |
| `kyc_watchlist_items` | قوائم الحظر/المراقبة الداخلية | `authenticated` فقط |
| `time_logs` | سجلات ساعات العمل القابلة للفوترة | `authenticated` فقط |
| `case_expenses` | مصروفات القضايا | `authenticated` فقط |
| `trust_transactions` | حركات **الحساب الائتماني/الأمانات (Trust Account)** | **الأعلى حساسية** — `authenticated` كحد أدنى فوري، ويوصى بشدة بإضافة عمود دور/صلاحية وربط policy بحيث لا يستطيع الكتابة (INSERT/UPDATE/DELETE) إلا حساب له دور "محاسب" أو "مدير" فقط، تماشياً مع متطلبات مسك حسابات الأمانة القانونية في الإمارات |
| `invoice_installments` | جدولة أقساط الفواتير | `authenticated` فقط |
| `str_reports` | تقارير الاشتباه (Suspicious Transaction Reports) لمكافحة غسل الأموال | **حساسية قصوى وسرية قانونية** — يوصى بقصر القراءة والكتابة على دور "مسؤول الامتثال/المدير" فقط عبر policy مخصصة، وليس فقط `authenticated` العام |
| `docs` | فهرس المستندات المؤرشفة | `authenticated` فقط |
| `poas` | الوكالات القانونية | `authenticated` فقط |
| `colleagues` | بيانات الزملاء والمحامين المتعاونين | `authenticated` فقط |
| `colleague_delegations` | الإنابات الصادرة للزملاء | `authenticated` فقط |
| `office_agreements` | اتفاقيات المكتب | `authenticated` فقط |
| `hearings` | الجلسات والرول | `authenticated` فقط |
| `tasks` | المهام الداخلية | `authenticated` فقط |
| `court_contacts` | دليل التواصل مع المحاكم | `authenticated` فقط (بيانات أقل حساسية لكن يُفضَّل عدم تركها عامة) |
| `internal_policies` | السياسات الداخلية | `authenticated` فقط |
| `disciplinary_actions` | إجراءات تأديبية بحق الموظفين | **حساسة (بيانات موارد بشرية)** — يُفضَّل قصرها على دور "مدير/HR" فقط للقراءة والكتابة |
| `judgment_deadlines` | مواعيد قانونية حاسمة (مواعيد الطعن ونحوها) | `authenticated` فقط |
| `consultation_bookings` | حجوزات الاستشارات المرئية وبيانات الدفع | `authenticated` فقط — تحتوي بيانات مالية/شخصية لعملاء محتملين |
| `consultation_settings` | إعدادات صف واحد فقط | `authenticated` فقط |

## ملاحظة هامة عن "المستخدمين والأدوار" (Users / RolePermissions)

من مراجعة الكود: حسابات المستخدمين وصلاحياتهم التفصيلية (`RolePermissions` في
`src/domain/permissions.ts`) لا تزال **مخزّنة محلياً في متصفح كل مستخدم فقط (localStorage,
المفتاح `firm_users`)** وليست في جدول Supabase مركزي بعد. هذا يعني:

1. لا يوجد جدول `users` في Supabase يحتاج RLS حالياً لهذا الغرض تحديداً (لأنه غير موجود أصلاً).
2. الفحص الوحيد الموثوق فعلياً على مستوى الخادم حالياً هو **تسجيل الدخول عبر Supabase Auth**
   (`requireSupabaseAuth` في `server/auth.ts`)، وتمت إضافة فحص دور إضافي (`requireServerRole`)
   يعتمد على حقل `app_metadata.role` في حساب Supabase Auth للمستخدم — وهو حقل **لا يمكن للمستخدم
   نفسه أو للمتصفح تعديله**، لأنه يتطلب صلاحية service-role (تُضبط يدوياً من لوحة تحكم Supabase
   أو عبر خادم موثوق).
3. **إجراء مطلوب من صاحب النظام**: لتفعيل هذا الفحص الإضافي فعلياً، يجب الذهاب إلى
   Supabase Dashboard → Authentication → Users → (كل مستخدم) → Edit → App Metadata، وإضافة
   `{"role": "admin"}` أو `{"role": "accountant"}` ...إلخ لكل حساب بحسب دوره الفعلي. حتى يتم
   ذلك يدوياً، ستُرفض الطلبات المحمية بـ `requireServerRole` بخطأ 403 حتى للمدير نفسه إن لم
   يُضبط له الدور.
4. **توصية للمرحلة القادمة (خارج نطاق هذه الدفعة من الإصلاحات)**: نقل جدول المستخدمين
   والصلاحيات بالكامل إلى جدول Supabase مركزي (وليس محلياً في كل متصفح)، مع RLS يسمح لكل
   مستخدم بقراءة صفّه فقط، وللمدير فقط بقراءة/تعديل الجميع — هذا ضروري لتحقيق تطبيق فعلي وكامل
   لنظام الصلاحيات على مستوى الخادم وليس فقط بالواجهة.

## خطوات التطبيق الموصى بها لكل جدول أعلاه

1. من لوحة تحكم Supabase: Database → Table Editor → اختر الجدول → تبويب "RLS".
2. تأكد أن "Enable RLS" مفعّل (وليس "RLS disabled").
3. أضف Policy جديدة باسم واضح، مثال:
   - `SELECT`: `USING (auth.role() = 'authenticated')`
   - `INSERT`: `WITH CHECK (auth.role() = 'authenticated')`
   - `UPDATE`: `USING (auth.role() = 'authenticated')`
   - `DELETE`: للجداول شديدة الحساسية (`trust_transactions`, `str_reports`,
     `disciplinary_actions`, `audit_logs`) يُفضَّل تقييد `DELETE` بشكل صريح (حتى لا يُحذف أي
     صف نهائياً من هذه الجداول عبر الواجهة الأمامية إطلاقاً)، ولإجراء أي حذف فعلي يتم ذلك
     يدوياً من لوحة تحكم Supabase مباشرة من قبل المدير.
4. كرر لكل جدول في القائمة أعلاه.
