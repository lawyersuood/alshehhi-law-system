# التعديلات المُنجزة — المرحلة الأولى (قبل الإطلاق)

هذا ملف موجّه للمحامي/صاحب النظام (غير التقني) يشرح بلغة بسيطة ماذا تغيّر في النظام ولماذا، بناءً
على نتائج المراجعة الفنية والقانونية السابقة.

## 1. تصحيح مشكلة "تضارب" عند تعديل البيانات من أكثر من جهاز في نفس الوقت

**المشكلة سابقاً**: عندما يعمل شخصان على النظام في نفس الوقت من جهازين مختلفين، كان يمكن أن يختفي
سجل أضافه أحدهما (مثلاً موكل جديد أو فاتورة جديدة) دون أن يحذفه أحد فعلياً، لمجرد أن الجهاز الآخر
رفع نسخته القديمة من البيانات التي لا تحتوي بعد على هذا السجل الجديد.

**الحل**: أصبح النظام الآن يتتبع فعلياً "ما الذي حذفه هذا المستخدم بنفسه" فقط، ولا يحذف أي سجل من
قاعدة البيانات المركزية إلا إذا كان هذا المستخدم قد حذفه بالفعل من شاشته. لم يعد النظام يحذف أي شيء
بناءً على مقارنة عمياء مع قائمة قديمة محلية. هذا يمنع فقدان بيانات أضافها زميل آخر في نفس الوقت.

**الملفات**: `src/domain/storageAndMessaging.ts`، `src/hooks/useSyncedTable.ts`، `src/App.tsx`.

## 2. توحيد طريقة ترقيم الفواتير

**المشكلة سابقاً**: كانت هناك 3 طرق مختلفة داخل الكود لتوليد رقم الفاتورة (كل طريقة تحسب الرقم
بأسلوب مختلف قليلاً)، مما قد يؤدي إلى ظهور فاتورتين برقم متطابق أو أرقام غير متسلسلة — وهذا مشكلة
محاسبية وقانونية (رقم الفاتورة الضريبية يجب أن يكون تسلسلياً وفريداً).

**الحل**: أصبح هناك مصدر واحد فقط لتوليد رقم الفاتورة، يعتمد دائماً على "أكبر رقم فاتورة موجود
فعلياً + 1"، بصرف النظر عن نقطة إنشاء الفاتورة (يدوياً، من تحويل ساعات عمل، أو من حجز استشارة).
هذا يضمن عدم تكرار الرقم مهما تعددت طرق إنشاء الفاتورة.

**الملفات**: `src/domain/storageAndMessaging.ts` (الدالة الجديدة `nextMainInvoiceNumber`)،
`src/App.tsx`.

## 3. توضيح أن بيانات "فحص العقوبات والأشخاص المعرّضين سياسياً (PEP)" هي إقرار ذاتي غير محقق

**المشكلة سابقاً**: كانت الواجهة تعرض حقل "فحص قوائم العقوبات" وحقل "PEP" وكأنهما نتيجة فحص حقيقي
وموثوق، بينما هما في الواقع مجرد اختيار يدوي يُدخله الموظف بنفسه، دون أي مطابقة آلية مع أي قائمة
عقوبات رسمية. هذا قد يُوهم فريق العمل أو الإدارة بأن العميل "تم فحصه فعلياً" بينما لم يحدث ذلك.

**الحل**: تمت إضافة عبارة "(إقرار ذاتي غير محقق)" بجانب هذه الحقول في كل مكان تظهر فيه (استمارة
إضافة/تعديل KYC، جدول سجلات KYC، الشارات الملونة)، مع تنبيه واضح مكتوب بالكامل يوضح أن هذا ليس
فحصاً آلياً، وأنه يجب على مسؤول الامتثال التحقق يدوياً من هذه البيانات مقابل مصادر رسمية قبل
الاعتماد عليها في أي قرار. لم يتم حذف الحقل أو تعطيله — فقط جُعلت الواجهة صادقة بشأن طبيعته الفعلية.

**الملفات**: `src/components/modals/KycModal.tsx`، `src/components/KycView.tsx`.

## 4. تنبيه مبدئي عن تعارض المصالح عند فتح قضية جديدة

**المشكلة سابقاً**: لم يكن هناك أي تحقق عند تسجيل قضية جديدة أو خصم جديد من كون هذا الاسم يطابق
موكلاً حالياً للمكتب أو خصماً سبق التعامل معه في قضية أخرى (وهو ما قد يشكّل تعارض مصالح يستوجب
تنبيه المحامي المسؤول قبل قبول التوكيل).

**الحل**: عند فتح نافذة "قيد قضية جديدة" وكتابة اسم الموكل أو أسماء الخصوم، يقوم النظام الآن تلقائياً
بمقارنة نصية (بدون حساسية لحالة الأحرف أو المسافات) مع أسماء كل الموكلين الحاليين وأسماء خصوم كل
القضايا الأخرى، وإن وُجدت مطابقة يظهر تنبيه واضح أعلى النافذة يوضح اسم كل تطابق ورقم القضية ذات
الصلة. هذا التنبيه **إرشادي فقط ولا يمنع الحفظ** — القرار النهائي يبقى بيد المحامي، لكنه يضمن عدم
إغفال احتمال تعارض واضح عن غير قصد.

**ملاحظة نطاق**: المطابقة نصية بسيطة (تطابق جزئي أو كامل للاسم) وليست مطابقة "ذكية" متقدمة تتحمل
الأخطاء الإملائية أو الأسماء المتشابهة صوتياً — وهذا كافٍ كخط دفاع أول حسب طلب المراجعة، ويمكن
تطويره لاحقاً إن رغب المكتب بذلك.

**الملفات**: `src/components/modals/CaseModal.tsx`، `src/App.tsx`.

## 5. بدء فرض الصلاحيات من جهة الخادم وليس فقط من واجهة المستخدم

**المشكلة سابقاً**: كانت كل الصلاحيات (من يحق له الحذف، من يحق له رؤية الماليات...) تُفرض فقط داخل
واجهة المتصفح. من الناحية الفنية، أي شخص لديه معرفة تقنية بسيطة يمكنه تجاوز هذا الفرض لأنه غير
موجود فعلياً على الخادم.

**الحل (ضمن النطاق المتاح لهذه المرحلة)**: تمت إضافة طبقة تحقق إضافية على مستوى الخادم (Express)
لأكثر العمليات حساسية في وحدة النظام المحاسبي: حذف الأصول الثابتة، حذف موظفي الرواتب، حذف
الموردين، الاطلاع على تفاصيل الرواتب والفواتير الضريبية، وتسجيل الحركات البنكية. يتحقق الخادم الآن
من "دور" المستخدم (admin/accountant/...) اعتماداً على بيانات محفوظة في حساب الدخول نفسه على
Supabase (حقل لا يستطيع المستخدم أو المتصفح تعديله بنفسه)، وليس من أي شيء يرسله المتصفح ضمن
الطلب — بحيث لا يمكن لمستخدم غير مخوّل تجاوز الفحص بمجرد تعديل الطلب من جهازه.

**نطاق التقليص (ولماذا)**: حسابات المستخدمين وصلاحياتهم التفصيلية في هذا النظام لا تزال مخزّنة محلياً
في متصفح كل مستخدم (وليس في قاعدة بيانات مركزية) — وهذا اكتشاف من هذه المراجعة نفسها. بناء نظام
صلاحيات كامل (RBAC) على الخادم يتطلب أولاً نقل جدول المستخدمين والأدوار إلى قاعدة بيانات مركزية،
وهذا تغيير معماري أكبر يتجاوز نطاق هذه الدفعة من الإصلاحات. لذلك تم التركيز على أعلى نقاط الخطورة
الموجودة فعلياً في كود الخادم (`server/`)، مع توثيق الخطوة اليدوية المطلوبة من صاحب النظام لتفعيل
الفحص (ضبط دور كل مستخدم من لوحة تحكم Supabase)، وترك بقية عمليات "الحذف" في النظام الرئيسي (مثل
حذف القضايا والموكلين) تحت مسؤولية سياسات RLS في Supabase (انظر الملف `RLS_CHECKLIST.md`)، لأن هذه
العمليات تتم مباشرة من المتصفح إلى قاعدة البيانات وليس عبر الخادم أصلاً.

**الملفات**: `server/auth.ts`، `server/routes/assets.ts`، `server/routes/payroll.ts`،
`server/routes/purchases.ts`، `server/routes/sales.ts`، `server/routes/bank.ts`.

## سياسات قاعدة البيانات (RLS) — إجراء مطلوب يدوياً من صاحب النظام

تم إعداد ملف منفصل `RLS_CHECKLIST.md` في جذر المشروع، يشرح بالتفصيل وباللغة العربية كل جدول من
جداول Supabase التي يعتمد عليها النظام (الموكلون، القضايا، الفواتير، KYC، سجل التدقيق، حسابات
الأمانة...)، وما هي سياسة الحماية (RLS) الموصى بتفعيلها لكل جدول من لوحة تحكم Supabase. هذه خطوة
**يجب تنفيذها يدوياً** من صاحب النظام لأن هذا الوكيل البرمجي لا يملك صلاحية الدخول للوحة تحكم
Supabase.

## التحقق الفني المُنجز

- فحص الأنواع (`npx tsc --noEmit`): بدون أي أخطاء.
- فحص جودة الكود (`npx eslint .`): بدون أي أخطاء (تحذيرات فقط، جميعها موجودة مسبقاً وغير متعلقة
  بهذه التعديلات).
- بناء الإنتاج (`npx vite build`): نجح بدون أخطاء.
- اختبارات المسارات الحرجة الآلية (Playwright، 8 اختبارات): جميعها ناجحة (تسجيل الدخول، القضايا،
  الموكلين، الفواتير، KYC، الجلسات، والتنقل العام بين الشاشات دون أي رسالة خطأ غير متوقعة).
