# المرحلة الرابعة — نقل أدوار المستخدمين إلى قاعدة بيانات مركزية (شرح مبسّط)

هذا الملف موجّه لمالك المكتب (وليس مبرمجاً)، ويشرح بلغة بسيطة: ما الذي تغيّر، لماذا هو مهم،
وما الذي يجب عليك فعله يدوياً خطوة بخطوة.

---

## ما المشكلة التي كانت موجودة؟

قبل هذا التحديث، "من هو المدير؟ من هو المحاسب؟ من هو السكرتير؟" كانت معلومة محفوظة فقط داخل
متصفح كل موظف (بيانات محلية على جهازه). هذا يعني أن قاعدة البيانات نفسها (Supabase) لم تكن تعرف
دور أي شخص، وبالتالي لم يكن هناك أي حماية حقيقية تمنع، مثلاً، موظفاً عادياً من الوصول لتقارير
الاشتباه بغسل الأموال أو الإجراءات التأديبية مباشرة عبر قاعدة البيانات، حتى لو كانت الشاشة نفسها
في الموقع تمنعه من رؤيتها. كل الحماية كانت "على الواجهة فقط"، وهذا غير كافٍ أمنياً.

## ما الذي تم عمله؟

1. **جدول مركزي حقيقي للأدوار**: تبيّن عند المراجعة أن جدولاً باسم `profiles` كان موجوداً
   بالفعل في قاعدة البيانات ويُستخدم جزئياً (لطلبات الانضمام الجديدة). تم اعتماده رسمياً
   كمصدر الحقيقة الوحيد لدور كل مستخدم، بدل إنشاء جدول مكرر جديد كان سيُسبب تضارباً.
2. **الخادم أصبح يقرأ الدور من هذا الجدول مباشرة** بدل الحاجة لتعديل يدوي في كل حساب من
   لوحة تحكم Supabase — أي أن تعيين دور موظف جديد الآن يتم من داخل شاشة "المستخدمون" في
   النظام نفسه، وسيُطبَّق فعلياً على مستوى قاعدة البيانات، وليس فقط شكلياً في الواجهة.
3. **حماية من تصعيد الصلاحيات**: أضفنا قيداً في قاعدة البيانات نفسها يمنع أي شخص من ترقية
   نفسه (أو أي شخص آخر) إلى "مدير" إلا إذا كان هو نفسه مديراً بالفعل. حتى لو حاول شخص خبيث
   التلاعب بالطلبات مباشرة (متجاوزاً واجهة الموقع)، ستمنعه قاعدة البيانات نفسها.
4. **حماية إضافية حقيقية على 3 جداول شديدة الحساسية**:
   - `str_reports` (تقارير الاشتباه بغسل الأموال): مقصورة الآن على "مدير" أو "مسؤول امتثال" فقط.
   - `disciplinary_actions` (الإجراءات التأديبية): مقصورة على "مدير" فقط.
   - `trust_transactions` (حساب الأمانات المالي): الكتابة فيها مقصورة على "مدير" أو "محاسب" فقط.

## لماذا هذا مهم؟

سابقاً كانت هذه الحماية كلها "ثقة" بأن الواجهة لن تسمح بالوصول. الآن أصبحت **حماية حقيقية
داخل قاعدة البيانات نفسها**، بحيث حتى لو تمكّن شخص من الوصول المباشر لقاعدة البيانات
(بمعرفة رابط ومفتاح المشروع العام، وهو مفتاح غير سري كما هو موضّح في `RLS_CHECKLIST.md`)، لن
يستطيع قراءة أو تعديل هذه البيانات الحساسة ما لم يكن دوره الفعلي المسجّل في قاعدة البيانات
يسمح بذلك.

---

## ما الذي يجب عليك فعله أنت الآن (خطوات إلزامية بالترتيب)

### الخطوة 1: تشغيل السكربت الجديد في Supabase

1. افتح لوحة تحكم Supabase الخاصة بالمشروع.
2. اذهب إلى **Database → SQL Editor → New query**.
3. افتح ملف `supabase_roles_migration.sql` (المرفق مع هذا التحديث) واقرأ **التحذير في أعلى
   الملف بعناية أولاً** — فيه خطوة يجب تنفيذها بمفردها قبل بقية السكربت، لضبط دورك أنت
   (مالك المكتب) كـ "مدير" أولاً، حتى لا يتم قفلك خارج قدرتك على إدارة الأدوار لاحقاً.
4. بعد التأكد من نجاح تلك الخطوة الأولى فقط، الصق بقية الملف كاملاً وشغّله.

### الخطوة 2: أعد تعيين دور كل موظف حالي، واحداً تلو الآخر

هذا هو الجزء **الأهم والذي لا بديل عنه**: بما أن أدوار الموظفين كانت محفوظة سابقاً فقط في
متصفح كل موظف، **لا توجد أي طريقة آلية آمنة لمعرفة الدور الصحيح لكل موظف حالي تلقائياً**.
لذلك، وبعد تشغيل السكربت:

- كل موظف حالي (عدا حسابك أنت الذي ضبطته يدوياً في الخطوة 1) سيظهر تلقائياً في النظام
  **بأدنى صلاحية ممكنة ("بدون صلاحيات خاصة")** حتى تراجعه أنت بنفسك. هذا مقصود ومتعمّد —
  لم نخمّن دور أي أحد، ولم نمنح أي أحد صلاحية "مدير" افتراضياً، حماية لك من أي خطأ.
- اذهب إلى شاشة **"المستخدمون"** في النظام، وراجع كل موظف واحداً تلو الآخر، وأعد إسناد
  دوره الصحيح (محامٍ، محاسب، سكرتير، مسؤول امتثال...) كما كان.
- إلى أن تراجع حساب موظف ما، ستكون صلاحياته محدودة جداً في النظام — هذا طبيعي ومتوقع خلال
  فترة المراجعة، وليس خللاً.

### الخطوة 3: لا حاجة لأي إعداد يدوي آخر لاحقاً

من الآن فصاعداً، عند إنشاء موظف جديد أو تعديل دور موظف حالي من شاشة "المستخدمون"، سيُطبَّق
الدور تلقائياً في قاعدة البيانات دون أي خطوة يدوية إضافية من لوحة تحكم Supabase — هذا هو
الفرق الجوهري عن الوضع السابق.

---

## مخاطر يجب أن تعرفها (بصراحة تامة)

- **هذا تغيير أعمق من التحديثات السابقة** لأنه يمس آلية الدخول والصلاحيات نفسها، وليس فقط
  شاشة أو تقريراً واحداً. تم اختباره بعناية (فحص الأنواع البرمجية، فحص الجودة، بناء كامل
  للموقع، وتشغيل كل اختبارات الفحص الآلي بنجاح)، لكن يُنصح بشدة أن تُجرّب تسجيل الدخول بنفسك
  وبموظف واحد آخر مباشرة بعد النشر، قبل إخبار بقية الفريق أن التحديث جاهز.
- **إن نسيت تنفيذ "الخطوة صفر" (ضبط دورك admin) قبل بقية السكربت**: ستفقد مؤقتاً القدرة على
  تغيير الأدوار من داخل الموقع (لكن ليس الدخول نفسه، ولا بياناتك). يمكن إصلاح هذا بسهولة
  بإعادة تنفيذ نفس استعلام "الخطوة صفر" من SQL Editor مباشرة في أي وقت.
- **إن لم تُشغّل السكربت في Supabase إطلاقاً**: لا شيء ينكسر — النظام يرجع تلقائياً لآلية
  التحقق القديمة (لأن الكود مكتوب ليتحمّل غياب الجدول أو الأعمدة الجديدة)، لكنك ببساطة لن
  تستفيد من الحماية الجديدة، وستبقى الثغرة الأمنية القديمة (RLS غير الدقيق) قائمة.
- **مراجعة أدوار الموظفين (الخطوة 2) هي مسؤوليتك أنت تحديداً** — نحن تعمّدنا عدم تخمين أي
  دور لأي أحد تفادياً لمنح صلاحيات خاطئة بالخطأ. إلى أن تُنجز هذه المراجعة، بعض الموظفين قد
  لا يستطيعون تنفيذ بعض المهام المعتادة حتى تُعيد إسناد دورهم.

## التراجع (Rollback) إن احتجت

- على مستوى الكود: هذا التحديث لا يحذف أي بيانات محلية قديمة، ويحتفظ بالمسار القديم كخطة
  بديلة تلقائية — لا حاجة لأي "تراجع" في الكود عادةً.
- على مستوى قاعدة البيانات: إن رغبت بإلغاء القيود الجديدة مؤقتاً، يمكن حذف الـ Trigger
  والسياسات الجديدة المذكورة أسماؤها بوضوح في `supabase_roles_migration.sql` (كل واحدة
  باسم `drop policy if exists "..."` أو `drop trigger if exists ...`) من SQL Editor، دون
  التأثير على البيانات نفسها.
