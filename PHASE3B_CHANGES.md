# المرحلة 3B — تقييد الوصول لبلاغات الاشتباه (STR / AML)

## الخلفية

بعد ترحيل الأدوار الأخير (انظر `PHASE4_ROLES_MIGRATION.md`)، أصبحت أدوار المستخدمين تُقرأ من جدول
`profiles` في Supabase عبر مفتاح الخدمة (Service Role)، بدل الاعتماد على `localStorage` فقط. هذا
يمنح النظام تحققاً حقيقياً على مستوى قاعدة البيانات (وليس واجهة المستخدم فقط).

كان هناك تعديل مخطَّط له سابقاً (في جلسة عمل منفصلة، على نسخة قديمة من الفرع) لتقييد الوصول إلى
سجل بلاغات الاشتباه (Suspicious Transaction Report — STR) بصلاحية حساسة مخصصة، لكنه لم يُدمج فعلياً
في هذا الفرع. هذا التعديل يُعيد تنفيذ نفس الفكرة، بشكل نظيف، فوق الوضع الحالي للكود بعد ترحيل الأدوار.

## ما تم تنفيذه (طبقة الواجهة — UI)

1. **صلاحية جديدة حساسة**: `manageStrReports` أُضيفت إلى:
   - `RolePermissions` في `src/domain/types.ts`
   - `DETAILED_ACTION_PERMISSIONS` و `PERMISSION_LABELS` في `src/domain/permissions.ts`، مع
     وسم `isSensitive: true` (نفس نمط صلاحية `manageLetterheadAssets` الحساسة الموجودة مسبقاً).
   - جميع `ROLE_PRESETS`:
     - `admin` (محامٍ شريك / مدير النظام): **مفعّلة**
     - `supervisor` (مشرف ومراجع إداري): **مفعّلة**
     - `lawyer`، `secretary`، `accountant`: **معطّلة** افتراضياً

2. **تقييد تبويب STR في `KycView.tsx`**: تبويب "سجل بلاغات الاشتباه AML / STR" لا يظهر إطلاقاً في
   شريط التبويبات إلا للمستخدم الذي يملك `manageStrReports` (أو مدير النظام). كذلك محتوى السجل نفسه
   محمي بنفس الشرط، حتى لا يظهر السجل لو تغيّرت الحالة الداخلية للتبويب المحدد.

3. **تقييد فتح نافذة "إضافة بلاغ اشتباه جديد"**: زر الفتح يستخدم الآن `openModalWithCheck("str",
   "manageStrReports")` بدل الفتح المباشر بلا فحص — بنفس النمط المستخدم في بقية النظام لجميع
   الإجراءات الحساسة الأخرى. كما أن حفظ البلاغ فعلياً في `saveStrReport` (داخل `App.tsx`) يتحقق الآن
   من `checkPerm("manageStrReports", ...)` بدل الاعتماد الخاطئ على صلاحية `manageKyc` العامة (التي
   يملكها كل من له حق التعامل مع KYC، وهو نطاق أوسع بكثير من فريق الامتثال).

4. **حماية إضافية على مستوى `App.tsx`**: نافذة `StrReportModal` لا تُعرض أصلاً إلا إذا كان المستخدم
   الحالي يملك الصلاحية (`canManageStrReports`)، كطبقة دفاع إضافية بجانب فحص `openModalWithCheck`.

5. **تنبيه توعوي بالعربية** ظاهر أعلى سجل بلاغات الاشتباه:
   > 🔒 الوصول إلى سجل بلاغات الاشتباه (STR) مقيّد ومحصور بمسؤول الامتثال أو مدير النظام فقط، حماية
   > لسرية بلاغات مكافحة غسل الأموال.

## طبقة قاعدة البيانات (RLS) — لا تعديل مطلوب

تم التحقق من ملف `supabase_roles_migration.sql`، وتبيّن أنه **يتضمن بالفعل** سياسة RLS حقيقية على
جدول `str_reports` (القسم 4 من الملف، سياسة `str_reports_admin_supervisor_all`) تقصر القراءة
والكتابة على الأدوار `admin` و `supervisor` فقط (بشرط `profiles.status = 'approved'`). لذلك **لم
يتم أي تعديل على ملف SQL** في هذه المرحلة — تقييد الواجهة أعلاه هو طبقة مكمّلة (Defense in Depth)
وليس الحماية الوحيدة:

- **الحماية الحقيقية والملزمة فعلياً هي RLS في قاعدة البيانات** (`supabase_roles_migration.sql`) —
  حتى لو تجاوز أحدهم واجهة المستخدم أو استدعى Supabase مباشرة، لن يتمكن من قراءة أو كتابة سجلات
  `str_reports` ما لم يكن دوره `admin` أو `supervisor` معتمداً.
- **تقييد الواجهة في هذه المرحلة** يمنع فقط ظهور التبويب والزر والنافذة لمن لا يملك الصلاحية، ويحسّن
  تجربة الاستخدام ويمنع الأخطاء العرضية، لكنه لا يُغني عن RLS ولا يحل محله.

## الملفات المعدّلة

- `src/domain/types.ts`
- `src/domain/permissions.ts`
- `src/App.tsx`
- `src/components/KycView.tsx`

## التحقق

- `npx tsc --noEmit` — بدون أخطاء
- `npx eslint .` — بدون أخطاء (تحذيرات فقط، جميعها سابقة على هذا التعديل)
- `npx vite build` — ناجح
- `npx playwright test tests/e2e/` — جميع الاختبارات (11) ناجحة
- `npx prettier --write` على كل الملفات المعدّلة
