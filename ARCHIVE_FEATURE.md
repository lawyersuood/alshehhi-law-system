# ميزة الأرشفة اليدوية — القضايا وملفات KYC

هذا الملف موجّه لصاحب المكتب (غير التقني) ليشرح بلغة بسيطة ماذا أُضيف للنظام، ويؤكد بشكل
صريح أن **هذه الميزة لا تحذف أي بيانات إطلاقاً**، بناءً على طلبك بعدم حذف أي شيء تلقائياً
لأسباب رقابية وإحصائية وأرشيفية.

## ما الذي تم إضافته؟

1. **زر "أرشفة"** يظهر بجانب كل قضية في شاشة "إدارة القضايا والرول"، وبجانب كل سجل في شاشة
   "اعرف عميلك (KYC)". الضغط عليه لا يفعل شيئاً أكثر من: وضع علامة داخلية على السجل نفسه
   (`archived = صحيح` وتاريخ الأرشفة)، بدون أي حذف أو نقل أو تعديل لأي بيانات أخرى في السجل.

2. **تبويبان في كل شاشة**: "النشطة" (أو "سجلات نشطة") و"الأرشيف". العرض الافتراضي يُظهر فقط
   السجلات غير المؤرشفة، حتى لا تزدحم الشاشة بالقضايا القديمة المنتهية، لكن السجلات المؤرشفة
   موجودة بالكامل ويمكن الاطلاع عليها في أي لحظة بالضغط على تبويب "الأرشيف".

3. **زر "إلغاء الأرشفة"** يظهر بدل زر الأرشفة على أي سجل مؤرشف بالفعل، ويعيد السجل فوراً إلى
   قائمة "النشطة" — وهذا يوضح عملياً أن الأرشفة إجراء تنظيمي قابل للتراجع الكامل، وليست خطوة
   نحو الحذف.

## ما الذي **لم** يتغيّر ولم يُحذف؟

- **لا يوجد أي حذف تلقائي أو مجدوَل** بناءً على مرور الوقت أو حالة القضية — الأرشفة تحدث فقط
  عندما يضغط المستخدم بنفسه على زر "أرشفة"، بقرار يدوي ومقصود، لكل سجل على حدة.
- **زر "حذف" الأصلي الموجود سابقاً** (لحذف قضية أو ملف KYC أُدخل بالخطأ مثلاً) لم يُمسّ ولم
  يُعدَّل، ويبقى يعمل تماماً كما كان — الأرشفة مضافة **بجانبه**، وليست بديلاً عنه.
- **السجل المؤرشف لا يُحذف من قاعدة بيانات Supabase ولا من الذاكرة/التخزين المحلي للتطبيق.**
  يبقى ضمن نفس المصفوفة (`cases` أو `kyc`) بكامل حقوله، فقط يحمل علامة `archived = صحيح`.
  آلية المزامنة الحالية (`useSyncedTable`) تتعامل معه كأي تحديث عادي لسجل موجود (وليس كحذف)،
  فيبقى محفوظاً في Supabase مثل أي سجل آخر ويظهر في كل التقارير والإحصائيات الإجمالية.
- **لا تأثير على الإحصائيات أو السجلات القانونية/المحاسبية**: كل الأرقام الإجمالية وسجل
  التدقيق (Audit Log) يستمران في احتساب كل السجلات، المؤرشفة وغير المؤرشفة على حد سواء —
  فقط طريقة العرض في الجدول هي التي تُخفي المؤرشف افتراضياً.

## خلاصة الضمان

**الأرشفة = تنظيم وترتيب فقط، وليست حذفاً بأي شكل.** أي سجل تتم أرشفته يبقى محفوظاً بالكامل
إلى الأبد (في قاعدة البيانات وفي التخزين المحلي)، ويمكن لأي مستخدم مخوّل رؤيته من تبويب
"الأرشيف" في أي وقت، وإعادته للعرض النشط بضغطة زر واحدة ("إلغاء الأرشفة") دون فقدان أي معلومة.

## الملفات التي تم تعديلها

- `src/domain/types.ts` — إضافة حقلي `archived` و`archivedAt` لنوعي القضية (`CaseItem`)
  وملف KYC (`KycItem`).
- `src/components/CasesListView.tsx` — أزرار الأرشفة/إلغاء الأرشفة وتبويب "الأرشيف" لشاشة
  القضايا.
- `src/components/KycView.tsx` — أزرار الأرشفة/إلغاء الأرشفة وتبويب "الأرشيف" لشاشة KYC.
