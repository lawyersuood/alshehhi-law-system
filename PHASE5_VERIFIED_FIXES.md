# المرحلة الخامسة — إصلاحات تم التحقق منها مباشرة في الكود المنشور فعلياً

## سبب هذا الملف

مراجعة تقنية وقانونية ثالثة اكتشفت أن إصلاحين مُعلَنين سابقاً في `PHASE3_CHANGES.md` (تطبيع
أسماء عربية لفحص تعارض المصالح، وتحذير رصيد الأمانة قبل الأرشفة) **لم يصلا فعلياً** إلى الفرع
المنشور على GitHub — تم بناؤهما في جلسة عمل منفصلة ولم يُدمَجا بنجاح مع النسخة التي رفعها صاحب
المكتب فعلياً. هذا الملف يوثّق الإصلاح الحقيقي الذي تم التحقق منه هذه المرة **مباشرة بقراءة
الكود بعد التعديل، وبتشغيل كل فحوصات التحقق من الصفر**، وليس فقط بالاعتماد على وصف الجلسة.

**الدرس المستفاد**: أي بند "تم ✅" في ملفات التغييرات السابقة يجب التحقق منه بقراءة الكود
مباشرة، وليس افتراض وصوله للفرع المنشور فعلياً لمجرد ذكره في ملف Markdown.

## الإصلاحات في هذه الدفعة (تم التحقق منها هذه المرة فعلياً)

### 1) فحص تعارض المصالح — تطبيع الأسماء العربية (تم فعلياً الآن)

`src/components/modals/CaseModal.tsx` كان لا يزال يستخدم دالة تطبيع ساذجة
(`s.trim().toLowerCase().replace(/\s+/g, " ")`) رغم التوثيق السابق. تم استبدالها الآن فعلياً
بدالة `normalizeArabicNameForMatch` الموجودة أصلاً في `src/domain/utils.ts` (تُعالج اختلاف
الهمزات إأآا، التاء المربوطة/الهاء، الألف المقصورة/الياء، التشكيل، والمسافات الزائدة). تم
التأكد بالقراءة المباشرة للملف بعد التعديل.

### 2) تحذير رصيد الأمانة قبل أرشفة قضية (تم فعلياً الآن)

`src/components/CasesListView.tsx` لم يكن يحتوي على أي منطق لحساب رصيد الأمانة قبل الأرشفة.
تمت إضافة:

- دالة `getCaseTrustBalance` تحسب رصيد الأمانة الفعلي للقضية من `trustTransactions` (تمريرها
  كخاصية جديدة من `App.tsx`).
- عند الضغط على "أرشفة" لقضية برصيد غير صفري، يظهر تحذير واضح بالمبلغ بالضبط، ويتطلب تأكيداً
  صريحاً (وليس منعاً نهائياً) للمتابعة رغم ذلك.
- سجل التدقيق (`logAuditAction`) يوثّق الآن صراحة ما إذا تمت الأرشفة بوجود رصيد مفتوح أم لا.

### 3) ثغرة "تراجع الدور" عند مراجعة موظف وخفض صلاحياته (إصلاح جديد اكتُشف في هذه المراجعة)

في `server/auth.ts`، كانت الدالة `fetchRoleFromProfilesTable` تُرجع `null` في حالتين مختلفتين
تماماً دون تمييز: (أ) لا يوجد صف بعد للمستخدم في `profiles` (حساب لم يُهاجَر)، و(ب) **يوجد صف
لكن حالته ليست `approved`** (أي مستخدم راجعه المدير وخفّض صلاحياته صراحة إلى `no_access`).
في كلتا الحالتين كان `requireServerRole` يقع في fallback إلى الدور القديم في
`app_metadata.role` — ما يعني أن خفض صلاحية موظف من شاشة "المستخدمون" لا يُطبَّق فعلياً على
الخادم إن كان لذلك الموظف دور قديم متبقٍ في `app_metadata` من قبل هذا الترحيل.

تم إصلاحها بتمييز واضح بين الحالتين: حساب "غير مهاجَر بعد" يُسمح فيه بالـfallback المؤقت،
بينما حساب "روجِع صراحة وخُفِّضت صلاحياته" يُرفض فوراً وقاطعاً (403) دون أي رجوع للدور القديم.

### 4) صف "يتيم" في profiles عند إنشاء مستخدم من الشاشة قبل تسجيل دخوله الفعلي (إصلاح جديد)

عند إنشاء موظف جديد من شاشة "المستخدمون" قبل أن يسجّل دخوله فعلياً عبر Supabase Auth، كان
يُنشأ له صف في `profiles` بمعرّف عشوائي لا يطابق `auth.users.id` الحقيقي. عند تسجيل دخوله
الفعلي لاحقاً، كانت آلية الفحص التلقائي تحاول إنشاء صف جديد بنفس البريد فتفشل بصمت بسبب قيد
التفرد على البريد، ويبقى الدور الذي عيّنه المدير غير مرئي أبداً للخادم.

تم إصلاحها: عند تسجيل الدخول الفعلي، إن وُجد صف "يتيم" بنفس البريد الإلكتروني بمعرّف مختلف،
يُربط هذا الصف بمعرّف تسجيل الدخول الحقيقي (تحديث `id`) بدل محاولة إدراج صف جديد فاشل، محافظاً
على الدور والصلاحيات المعيّنة مسبقاً.

### 5) نوع الدور (`roleKey`) في `saveUser` كان يستبعد "supervisor" (إصلاح صغير)

كان الـcast في `App.tsx` يحصر القيم الممكنة بـ `admin | lawyer | secretary | accountant`
بدون `supervisor`، رغم أن هذا الدور موجود فعلياً في `ROLE_PRESETS` ومُستخدم في سياسات RLS
لجدول `str_reports`. تم تصحيح الـcast ليشمل `supervisor` أيضاً.

## التحقق الكامل بعد هذه الدفعة

- `npx tsc --noEmit`: 0 أخطاء
- `npx eslint .`: 0 أخطاء (574 تحذيراً سابقاً، لا جديد)
- `npx vite build`: نجح
- Playwright (`tests/e2e/`): 11/11 ناجحة (شُغِّلت مرتين: قبل وبعد تنسيق Prettier)
- `npx prettier --write` طُبِّق على كل الملفات المعدَّلة

## ملاحظة صادقة متبقية (لم تُعالَج في هذه الدفعة عمداً)

مراجعة الطرف الثالث (القانونية) أشارت أيضاً إلى غياب أي سجل تدقيق لعمليات "الاطلاع فقط" على
بلاغات STR (وليس فقط من أنشأ/عدّل البلاغ). هذا تحسين حقيقي إضافي يستحق التنفيذ لاحقاً لكنه لم
يُدرَج في هذه الدفعة لتفادي توسيع النطاق أكثر مما يمكن التحقق منه بدقة في جلسة واحدة — يوصى
بإضافته كخطوة تالية منفصلة.
