// ============================================================
// سجل العملاء المستقل (Customers) — قسم المبيعات
// سجل بيانات تواصل للعملاء (اسم، رقم ضريبي، تواصل) يُستخدم كمرجع سريع عند إصدار
// عروض الأسعار والفواتير. غير مرتبط حالياً بمعرّف حقيقي على مستوى الفاتورة
// (لا تزال الفاتورة تخزّن اسم العميل كنص حر clientName) — المطابقة تتم بالاسم فقط،
// تماشياً مع عزل هذا النظام عن نظام القضايا والموكلين الأساسي في الموقع.
// ============================================================

export interface Customer {
  id: string;
  name: string;
  trn?: string; // الرقم الضريبي للعميل
  phone?: string;
  email?: string;
  notes?: string;
  isActive: boolean;
  createdAt: string;
}

export const CUSTOMER_LS_KEYS = {
  customers: "firm_accounting_customers_v1",
};
