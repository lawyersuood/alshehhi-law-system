# المرحلة السادسة — التحسينان الأولان من المراجعة الطموحة

بناءً على المراجعة التي نظرت للنظام من "عين المبرمج الطموح" و"عين المحامي الواعي"، تم تنفيذ أعلى
تحسينين تأثيراً من الناحية القانونية المهنية، والمبنيان مباشرة على بنية تحتية موجودة أصلاً (KYC
وTrustTransactionModal) دون الحاجة لأي بناء من الصفر.

## 1) فحص تعارض المصالح "من الدرجة الثانية" المبني على المستفيد الحقيقي (UBO)

**المشكلة السابقة:** فحص تعارض المصالح كان يعتمد فقط على مطابقة نصية بين اسم الخصم واسم الموكل
مباشرة. في الممارسة الإماراتية الحقيقية، كثيراً ما يكون التعارض الفعلي عبر هيكل ملكية معقد — الخصم
قد يكون هو المالك المستفيد (UBO) لموكل قائم، أو قد يشترك موكلان في نفس المالك المستفيد (شركات تابعة
لنفس المجموعة الاقتصادية) دون أن تتشابه أسماؤهما إطلاقاً.

**ما تم تنفيذه** (`src/domain/utils.ts`, `src/components/modals/CaseModal.tsx`):
- دالة جديدة `extractCandidateNamesFromText` تستخرج أسماء مرشّحة من حقل UBO النصي الحر في ملف
  KYC (تقسيم على الفواصل الشائعة + تطبيع الأسماء العربية بنفس دالة التطبيع المستخدمة أصلاً).
- عند فتح نافذة "قيد قضية جديدة"، يتم الآن أيضاً:
  1. مطابقة اسم كل خصم مُدخل مع الأسماء المستخرجة من حقل UBO لجميع الموكلين — فإن تطابق، يظهر
     تنبيه بأن الخصم قد يكون مالكاً مستفيداً أو طرفاً ذا صلة بموكل قائم.
  2. فحص ما إذا كان الموكل المختار يشترك في نفس المستفيد الحقيقي مع موكل آخر، وهذا الموكل الآخر
     طرف خصومة في قضية أخرى — تنبيه بمراجعة تعارض المصالح على مستوى المجموعة الاقتصادية.
- **هذا فحص إرشادي (heuristic) غير حاسم** لأن حقل UBO نص حر وليس بيانات هيكلية — لا يمنع الحفظ،
  بل ينبّه المحامي ليراجع يدوياً قبل قبول التوكيل، تماماً بنفس فلسفة الفحص الأساسي الموجود سابقاً.

## 2) تسوية حساب الأمانات (Trust Reconciliation)

**المشكلة السابقة:** لم يكن هناك أي آلية لمقارنة رصيد دفاتر المكتب لحساب الأمانات مع رصيد كشف
الحساب البنكي الفعلي — وهذه من أخطر المخاطر التأديبية الفردية التي يواجهها أي محامٍ (مشابهة
لمخالفات IOLTA في الأنظمة الغربية).

**ما تم تنفيذه:**
- نوع بيانات جديد `TrustReconciliation` (`src/domain/types.ts`) وجدول متزامن جديد
  `trustReconciliations` (`src/hooks/useBillingRecords.ts`، جدول Supabase باسم
  `trust_reconciliations` — **راجع ملف `trust_reconciliation_table.sql` المرفق لإنشائه**).
- لوحة "تسوية حساب الأمانات" جديدة أعلى قسم الأمانات في شاشة الفواتير (`InvoicesView.tsx`):
  تعرض رصيد الدفاتر الإجمالي تلقائياً، وحقل لإدخال رصيد كشف البنك الفعلي، ويُحسب الفارق فوراً
  ويُصنَّف (مطابقة تامة / يوجد فارق يستوجب المراجعة)، مع سجل كامل لكل التسويات السابقة
  (لا تُحذف أو تُعدَّل أي تسوية سابقة — سجل تدقيقي دائم).
- تنبيه فوري أعلى اللوحة إن وُجد أي موكل برصيد أمانة سالب حالياً.
- **تنبيه وقائي عند إدخال معاملة صرف أمانة** (`saveTrustTransaction` في `App.tsx`): إن كانت
  المعاملة ستجعل رصيد أمانة الموكل سالباً (أي صرف يفوق المودَع فعلياً)، يظهر تنبيه صريح بالمبلغ
  والرصيد الناتج قبل الحفظ، مع تسجيل الأمر في سجل التدقيق سواء تم التراجع أو المتابعة.

## خطوة واحدة مطلوبة منك قبل الرفع

نفّذ ملف `trust_reconciliation_table.sql` المرفق في Supabase SQL Editor (نفس الطريقة المتبعة في
كل مرة سابقة) — خطوة بسيطة ومنفصلة تماماً عن ترحيل الأدوار، تُنشئ جدولاً واحداً جديداً فقط.

## التحقق

- `npx tsc --noEmit`: نظيف بالكامل (صفر أخطاء).
- `npx eslint`: صفر أخطاء جديدة (فقط تحذيرات موجودة مسبقاً في الملفات المعدَّلة).
- `npm run build`: ناجح.
- Playwright (11 اختباراً): جميعها ناجحة، بما فيها اختبار معاملة الأمانة (مرّ مرتين متتاليتين
  للتأكد من الاستقرار).
