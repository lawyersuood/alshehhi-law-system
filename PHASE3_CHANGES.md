# المرحلة الثالثة — إغلاق ثغرات التدقيق الأمني (الجولة الثانية)

هذا الملف يوثّق التعديلات التي نُفّذت رداً على نتائج جولة التدقيق الثانية بعد إصلاحات المرحلتين
الأولى والثانية. جميع التعديلات على مستوى الكود فقط، ولا تتطلب أي تغيير في إعدادات لوحة تحكم
Supabase أو سياسات RLS (تلك تبقى مسؤولية يدوية موثّقة في `RLS_CHECKLIST.md`).

## 1) إغلاق فجوة التحقق من الصلاحيات على مستوى الخادم (الأولوية الأعلى)

**المشكلة:** بعض نقاط النهاية (endpoints) في وحدات النظام المحاسبي على الخادم (`server/routes/`)
كانت محمية فقط بـ `requireSupabaseAuth` — أي أن أي مستخدم مسجّل دخوله (بغض النظر عن دوره) كان
يستطيع تنفيذها عبر استدعاء مباشر لواجهة البرمجة (API)، متجاوزاً القيود التي تفرضها واجهة
المستخدم فقط. كانت عمليات الحذف (`DELETE`) وبعض عمليات `GET` الحساسة محمية بشكل صحيح عبر
`requireServerRole(...)`، لكن عمليات `POST`/`PUT` الأخطر (اعتماد فواتير، تشغيل رواتب، صرف رواتب،
إنشاء/تعديل حسابات بنكية، استبعاد أصول ثابتة) لم تكن محمية بنفس الطريقة.

**الإصلاح:** أُضيفت طبقة `requireServerRole("admin", "accountant")` (نفس الأدوار المستخدمة أصلاً
في نقاط الحذف بهذه الملفات نفسها) على نقاط النهاية التالية:

- `server/routes/payroll.ts`: `POST /payroll-runs`, `POST /payroll-runs/:id/pay`
- `server/routes/purchases.ts`: `POST /purchase-invoices/:id/approve`, `POST /purchase-invoices/:id/cancel`
- `server/routes/sales.ts`: `POST /sales-invoices/:id/approve`, `POST /sales-invoices/:id/cancel`, `POST /sales-payments`
- `server/routes/bank.ts`: `POST /bank-accounts`, `PUT /bank-accounts/:id`
- `server/routes/assets.ts`: `POST /fixed-assets/:id/dispose`

تم اختيار الدورين `admin` و`accountant` لأنهما نفس الدورين المستخدمين مسبقاً على نقاط الحذف
والاطلاع الحساسة في هذه الملفات ذاتها (مثال: حذف حساب بنكي، حذف قيد إهلاك، `GET /payslips`)،
مما يحافظ على اتساق نموذج الصلاحيات دون ابتكار أسماء أدوار جديدة. الدور يُقرأ حصراً من
`app_metadata.role` في Supabase Auth (حقل لا يمكن للمستخدم أو المتصفح تزويره)، تماماً كما هو
موثّق في `server/auth.ts`.

لم تُغيَّر أي نقطة نهاية كانت تعمل بشكل صحيح مسبقاً — تم قراءة كل ملف بالكامل قبل التعديل
والتأكد من أن التوقيع والمنطق الداخلي لكل معالج (handler) لم يتغيرا، فقط أُضيف middleware إضافي
في السلسلة.

## 2) تحسين دقة فحص تعارض المصالح (Conflict of Interest) بالأسماء العربية

**المشكلة:** فحص تعارض المصالح في `src/App.tsx` (عند حفظ قضية جديدة) كان يقارن الأسماء بطريقة
بسيطة جداً: `.trim().toLowerCase()`. هذا لا يعالج اختلافات الأسماء العربية الشائعة، مثل صور
الهمزة المختلفة (أ/إ/آ/ا)، التاء المربوطة مقابل الهاء، الألف المقصورة مقابل الياء، التشكيل،
أو المسافات الزائدة — مما قد يسمح بمرور تعارض مصالح فعلي دون أن يُكتشف لمجرد اختلاف طفيف
في كتابة الاسم.

**الإصلاح:** استُبدلت المقارنة الساذجة باستخدام الدالة الموجودة أصلاً في `src/domain/utils.ts`
باسم `normalizeArabicNameForMatch` (وهي نفس الدالة المستخدمة في وحدات أخرى مثل اعرف عميلك KYC
والبحث)، في موضعين:

- عند التحقق مما إذا كان الموكل الجديد قد ظهر كخصم سابق في قضية أخرى.
- عند التحقق مما إذا كان أحد خصوم القضية الجديدة مسجّلاً بالفعل كموكل حالي للمكتب.

هذا يجعل الفحص يتعرف على تطابق الأسماء رغم اختلاف صور الهمزة أو التاء المربوطة/الهاء أو الألف
المقصورة/الياء أو المسافات الزائدة، بدلاً من الاعتماد على تطابق حرفي تام بعد تحويل الأحرف لأحرف
صغيرة فقط.

## 3) تحذير إلزامي عند أرشفة قضية برصيد أمانة غير مُسوّى

**المشكلة:** زر الأرشفة ("أرشفة") في `src/components/CasesListView.tsx` كان يقوم مباشرة بتعيين
`archived: true` دون أي فحص لوضع القضية المالي. إذا كانت القضية تحتفظ برصيد أمانة (Trust Account)
غير مُسوّى (إيداعات لم تُصرف بالكامل، أو صرف يفوق الإيداع)، فإن أرشفتها بصمت قد يؤدي إلى نسيان
هذا الرصيد ومخاطر مهنية/مالية لاحقاً.

**الإصلاح:** أُضيف حساب `caseTrustBalance` داخل `CasesListView.tsx` يجمع كل معاملات الأمانة
(`trustTransactions`) الخاصة بالقضية تحديداً (وليس فقط بالموكل)، بحيث: الإيداع ("إيداع أمانة")
يُضاف، وكل أنواع الصرف/الاسترداد الأخرى ("صرف رسوم محكمة"، "صرف أتعاب خبرة"، "استرداد للموكل")
تُطرح. إذا كان الرصيد الناتج غير صفري (بهامش تقريب صغير)، يظهر للمستخدم تحذير صريح برسالة تتضمن
قيمة الرصيد بالدرهم الإماراتي، ويُطلب تأكيده صراحة (`window.confirm`) قبل المتابعة بالأرشفة.

تم تصميم هذا كـ"تحذير قابل للتجاوز" وليس حجباً كاملاً دائماً — بحيث لا يبقى صاحب المكتب عالقاً
غير قادر على أرشفة قضية قديمة لسبب إداري، لكن لا يمكن لأي مستخدم أن يؤرشف قضية برصيد أمانة مفتوح
عن طريق الخطأ دون أن يرى هذا التحذير أولاً. كما يُسجَّل في سجل التدقيق (Audit Log) بوضوح متى
تمت الأرشفة رغم وجود رصيد أمانة غير مُسوّى، ليبقى الأمر موثّقاً.

## التحقق

- `npx tsc --noEmit` — بدون أخطاء.
- `npx eslint .` — بدون أخطاء (تحذيرات موجودة مسبقاً في الكود ولا علاقة لها بهذه المرحلة).
- `npx vite build` — نجح البناء.
- تشغيل كامل مجموعة اختبارات Playwright (`tests/e2e/`) — جميع الاختبارات الـ11 نجحت.
- تم تشغيل `npx prettier --write` على كل ملف تم تعديله في هذه المرحلة.

## خارج نطاق هذه المرحلة

لم يُلمس أي شيء متعلق بسياسات RLS في لوحة تحكم Supabase — هذا يبقى مسؤولية يدوية لصاحب المكتب،
وموثّق مسبقاً بالتفصيل في `RLS_CHECKLIST.md` (بما في ذلك ملاحظة حساسية جدول `str_reports`).
